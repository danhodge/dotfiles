;;; init-browse-kill-ring.el --- browse-kill-ring configuration

;;; Commentary:

;;; Code:

(use-package browse-kill-ring
  :ensure t
  :bind (("M-y" . browse-kill-ring)))

(provide 'init-browse-kill-ring)
;;; init-browse-kill-ring.el ends here
